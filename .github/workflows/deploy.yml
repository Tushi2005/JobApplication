name: Auto Deploy to Portainer

on:
  push:
    branches:
      - master

jobs:
  deploy:
    
    runs-on: self-hosted 

    steps:
      - name: Trigger Portainer Webhook
        run: |
          curl -k -X POST "https://192.168.1.108:31015/api/stacks/webhooks/6eeb98f1-4eaf-4503-8239-96034cd1b1e9"
          