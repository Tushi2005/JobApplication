services:
  backend:
    image: adamtuska/jobtracker-backend:latest
    container_name: jobtracker-backend

    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - ix-postgres_default
networks:
  ix-postgres_default:
    external: true